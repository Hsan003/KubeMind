services:
#  app:
#    build: .
#    ports:
#      - "8000:8000"
#    env_file: .env
#    depends_on:
#      - loki
#    volumes:
#      - ~/.kube:/home/app/.kube:ro   # mount user's kubeconfig read-only

  loki:
    image: grafana/loki:2.9.0
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml

  grafana:                           # optional: visualize logs during dev
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    depends_on:
      - loki